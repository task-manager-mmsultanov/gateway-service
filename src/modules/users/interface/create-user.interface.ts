/**
 * Represents the interface for creating a user.
 */
export interface CreateUserInterface {
    first_name: string
    last_name: string
    email: string
    password: string
    role_id: number
}
